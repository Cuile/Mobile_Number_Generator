version: "3.7"
services:
  mobile_number_generator:
    image: mobile_number_generator
    build: 
      dockerfile: ./Dockerfile
      target: mobile_number_generator
    volumes:
      - ../src:/root/src/:ro
      - ../data:/root/data/:rw
    working_dir:  /root/
    tty: true
